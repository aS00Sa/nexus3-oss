---
# Сборка nexus_repos_apt_proxy из списков 04–06 + GitLab CE (06-apt-gitlab-ce-repos.yml).
nexus_config_apt: true
nexus_repos_apt_proxy: "{{ nexus_apt_proxy_ubuntu | default([]) + nexus_apt_proxy_debian_12 | default([]) + nexus_apt_proxy_debian_13 | default([]) + nexus_apt_proxy_gitlab_ce | default([]) }}"
