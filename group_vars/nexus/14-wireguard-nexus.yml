---
# WireGuard на хосте Nexus: конфиг templates/WG1.conf (локально, в .gitignore).
# Split-tunnel: /etc/wireguard/wg1.domains и wg1.static на хосте (деплой не перезаписывает).
nexus_wireguard_enable: true
